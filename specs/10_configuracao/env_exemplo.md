# Variáveis de ambiente

Criar `.env` fora do versionamento:
```dotenv
YOUTUBE_API_KEY=
MINIO_ENDPOINT=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MLFLOW_TRACKING_URI=
```
Nunca colocar chaves no YAML, logs ou repositório.
